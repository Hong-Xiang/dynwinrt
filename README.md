# dynwinrt
Use WinRT methods dynamically

Build with `cargo build`

## Use WinAppSDK Bootstrap

The path to the WinAppSDK Bootstrap DLL is now retrieved from an environment variable named `WINAPPSDK_BOOTSTRAP_DLL_PATH`.